#!/bin/bash
set -euo pipefail

# Attach the terminal so copy-shared-files can prompt, but don't fail when
# there is no TTY (IDE integrations, CI).
if sh -c ': < /dev/tty' 2> /dev/null; then
  exec < /dev/tty
fi
npm run copy-shared-files
