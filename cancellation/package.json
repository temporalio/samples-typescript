{
  "version": "0.1.0",
  "private": true,
  "name": "temporal-cancellation",
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "build.watch": "tsc --build --watch ./tsconfig.json",
    "schedule-workflow": "node ./lib/starter",
    "worker": "node ./lib/worker"
  },
  "dependencies": {
    "temporalio": "0.4.x"
  },
  "devDependencies": {
    "typescript": "^4.4.2",
    "@tsconfig/node14": "^1.0.0"
  }
}
