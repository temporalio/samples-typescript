{
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "build.watch": "tsc --build --watch ./tsconfig.json",
    "worker": "node ./lib/worker",
    "workflow-fast": "node ./lib/starter/fast",
    "workflow-slow": "node ./lib/starter/slow"
  },
  "name": "temporal-timer",
  "dependencies": {
    "dotenv": "^10.0.0",
    "mailgun-js": "^0.22.0",
    "temporalio": "0.11.x"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.0",
    "@types/mailgun-js": "0.22.12",
    "typescript": "^4.4.2"
  }
}
